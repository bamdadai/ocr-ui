# ==================================
# GPU-Enabled Worker Service Dependencies
# ==================================

# Include all shared packages from base.txt
-r base.txt

# --- ML/DL & Image Processing (GPU-enabled versions) ---
# Pinning versions is crucial for stability in ML libraries. 
pytorch-lightning==2.5.2 
ultralytics==8.3.73
opencv-python==4.9.0.80
transformers==4.50.1
numpy==1.26.4
pillow==11.3.0
pdf2image==1.17.0
timm==1.0.16
matplotlib==3.10.3
scikit-image==0.24.0

# --- PaddleOCR (ppocr module) Dependencies ---
pyclipper==1.3.0.post5
shapely==2.0.6
PyYAML==6.0.2
imgaug==0.4.0
albumentations==1.4.0

# --- GPU-specific packages ---
# Add any GPU-specific packages here if needed

# --- Worker-Specific Utilities ---
requests==2.32.4
psutil==7.0.0
nltk==3.8.1 
# --- Persian Text & PDF Generation ---
python-bidi==0.6.6
arabic-reshaper==3.0.0
fpdf==1.7.2
lmdb
