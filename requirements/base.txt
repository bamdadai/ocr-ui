# ==================================
# Base Dependencies (Core packages only)
# ==================================

# Core framework dependencies
celery[redis]==5.5.3
pydantic==2.8.2
pydantic-settings==2.4.0
python-dotenv==1.1.1
python-json-logger==2.0.7
structlog==25.4.0

# Fast compression for image storage (3-10x faster than zlib)
zstandard==0.23.0

# Hybrid storage for large file offloading to MinIO
minio==7.2.4
